import 'package:flutter/material.dart';

import '../widgets/products_menu_scaffold.dart';

class ProductsMenuRoute extends StatelessWidget {
  static const String route = 'products_menu';
  @override
  Widget build(BuildContext context) {
    return ProductsMenu();
  }
}
